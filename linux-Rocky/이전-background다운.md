# fedora 이전 버전 background

이전 버전 background 받기  

GNOME 에서는 
```
sudo dnf install f43-backgrounds-gnome
```

해주면 간단하게 workstation 44 에서도 이전 버전의 background 를 사용할 수 있다.  

>  KDE Plasma  
`sudo dnf install f43-backgrounds-kde`


