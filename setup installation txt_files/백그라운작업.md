
$nohup
(no hang up)

nohup 을 사용해서 계속 백그라운드에서 작업하게 하기
백 그라운드에서 작업이 되게 함
$nohup streamlit run app.py &

이제 exit로 AWS 에서 빠져나와도 백그라운드에서 계속 돌아가니깐 서버가 계속 운영되게 됨

작동 중지 시키기 (  | (파이프)와 grep으로 검색하기)
$ps -ef | grep stream 검색
간단하게 ps -e | grep stream 도 괜찮음

위에서 나온 번호로 kill 명령어로 끄기
예
$kill 3274
