<?php 
  $stringexample = 'Hello World';
  
  // built-in functions
  //echo str_shuffle($stringexample);  //shuffle(randomly) string
  //var_dump(str_split($stringexample)); //split string
  // split words into indiviual character in the array

  //echo str_replace("World", "joel", $stringexample);  // upper case, lower case strickly 
  //echo str_ireplace("world", "joel", $stringexample); // it doen't matter either upper or lower case
  
  //echo str_word_count($stringexample); //it returns counts of words
  //echo strlen($stringexample); // calculates total characters of variable 

  // how to use string
//  echo "Hello World Again" . $stringexample . "\n";  //add comma(.) 

//echo 'I\'m back';  // put back-slash(\) behind single quote(')

//  echo "This is a Quote \"Talk is Cheap, show me the code\"" . "\n";
    //put back-slash(\) behind double quote(")
//  echo "I'm Back" . "\n";
//  echo 'This is a Quote "Talk is Cheap, show me the code"' . "\n";
     // it's okay to use double qoute(") inside single quote (')

    trim($temp) - 공백제거